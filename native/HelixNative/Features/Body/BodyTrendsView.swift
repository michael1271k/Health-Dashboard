import SwiftUI
import HelixUI
import HelixCore
import HelixData

// Wave 7 stub — filled in by the Body & Vitals agent. Mounted from DayTabView.
struct BodyTrendsView: View {
    var body: some View {
        HelixChartEmpty("Coming in this wave.")
            .helixScreen(.body)
            .navigationTitle("Trends")
    }
}
