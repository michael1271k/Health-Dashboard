import SwiftUI
import HelixUI
import HelixCore
import HelixData

// Wave 7 stub — filled in by the Training Trends agent. Mounted from TrainTabView.
struct TrainingTrendsView: View {
    var body: some View {
        HelixChartEmpty("Coming in this wave.")
            .helixScreen(.train)
            .navigationTitle("Trends")
    }
}
