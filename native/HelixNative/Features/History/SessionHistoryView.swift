import SwiftUI
import HelixUI
import HelixCore
import HelixData

// Wave 7 stub — filled in by the Workout Analysis agent. Mounted from TrainTabView.
struct SessionHistoryView: View {
    var body: some View {
        HelixChartEmpty("Coming in this wave.")
            .helixScreen(.train)
            .navigationTitle("History")
    }
}
