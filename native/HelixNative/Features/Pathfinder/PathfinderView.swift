import SwiftUI
import HelixUI
import HelixCore
import HelixData

// Wave 7 stub — filled in by the Pathfinder agent. Mounted from YouTabView.
struct PathfinderView: View {
    var body: some View {
        HelixChartEmpty("Coming in this wave.")
            .helixScreen(.train)
            .navigationTitle("Pathfinder")
    }
}
