<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<!-- EXPORT COMPLIANCE. Absent, every App Store Connect upload stops on the
	     encryption question and waits for it to be answered by hand, per build.
	     `false` is the correct answer here and not a shortcut: the only
	     cryptography in this app is HTTPS/TLS through the system frameworks
	     (Supabase over HTTPS, the Netlify deploy, the widget token in an
	     Authorization header). There is no bundled crypto implementation and
	     nothing proprietary, which is exactly the exemption this key claims. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<!-- Deep links (helix://open?path=/nutrition). The widget extension that
	     emitted them moved to native/ in Wave 5; the scheme stays registered —
	     see src/lib/native/deepLink.ts. -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>app.helix.health.michael</string>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>helix</string>
			</array>
		</dict>
	</array>
	<key>CFBundleDisplayName</key>
	<string>Helix</string>
	<!-- Live Activities. This key is the whole entitlement: ActivityKit needs no
	     paid capability for LOCAL updates, which is the only kind Helix sends
	     (see HelixLiveActivityPlugin — `pushType: nil`). Without it
	     `ActivityAuthorizationInfo().areActivitiesEnabled` is false forever and
	     `Activity.request` throws, silently, on a device that supports it. -->
	<key>NSSupportsLiveActivities</key>
	<true/>
	<!-- Frequent local updates. Off by default; without it iOS throttles
	     `activity.update` hard enough that ticking sets in quick succession
	     stops moving the Lock Screen card. -->
	<key>NSSupportsLiveActivitiesFrequentUpdates</key>
	<true/>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSApplicationCategoryType</key>
	<string></string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>NSHealthShareUsageDescription</key>
	<string>HELIX reads Apple Health (steps, energy, heart rate, HRV, sleep, body composition) to compute your daily readiness and trends</string>
	<key>NSHealthUpdateUsageDescription</key>
	<string>HELIX does not write to Apple Health</string>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>arm64</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>
</dict>
</plist>
